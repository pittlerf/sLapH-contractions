#!/bin/bash
# Copyright © 2017 Martin Ueding <dev@martin-ueding.de>

set -e
set -u
set -x

tmp="$(mktemp)"

for path in correlators-20171113/*.h5; do
    basename="${path##*/}"

    if ! h5diff -r -d 1e-14 "$path" "correlators/$basename" &> "$tmp"; then
      cat "$tmp"
    fi
done

rm -f "$tmp"
